<h2>View Map Stories</h2>
<div class='story-index'>
  <% @stories.each do |story| %>
    <div class='widget story'>
      <%= link_to story.name, story_path(story) %>
      <div class='story-description'>
        <p><%= story.description %></p>
      </div>
    </div>
  <% end %>
</div>